{
  "name": "task-lake",
  "version": "0.1.0",
  "description": "A deterministic local task CLI designed for humans and AI agents",
  "type": "module",
  "private": true,
  "bin": {
    "tlk": "./src/index.ts"
  },
  "scripts": {
    "tlk": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^5.9.3"
  }
}
